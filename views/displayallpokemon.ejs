<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" type="text/css" href="styles.css">
    <title>All pokemon</title>
  </head>
  <body>

    

    <div class="pokemon-container">
      <% allPokemons.forEach((pokemon) => { %>

      <div class="pokemon-div">


        <img class="poke-img"  src="<%= pokemon.img %>" alt="<%= pokemon.name %>" />
        
        <p class="poke-id">Id: <%= pokemon.pokeId %></p>
        
        <p>Name: <a class="poke-name" href="<%= pokemon.pokeUrl %>"><%= pokemon.name %></a></p>
        
        <p class="poke-type">type: <%= pokemon.type.join(", ") %></p>
      
      
      </div>

      <% }) %>
    </div>
    </div>
  </body>
</html>
